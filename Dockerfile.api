FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt pyproject.toml ./
RUN pip install --no-cache-dir -e .

COPY src/ ./src/

CMD ["uvicorn", "bim_graph.api.main.app", "--host", "[IP_ADDRESS]", "--port", "8000"]

